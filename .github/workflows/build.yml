name: Build

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  build-and-gametest:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out Remnant
        uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Check out pinned TotemCore
        uses: actions/checkout@v4
        with:
          repository: Yunitrish006006/TotemCore
          ref: 5be940a1fb91d250bb6781193bd49f0f24b40a43
          path: .lockstep/TotemCore
          persist-credentials: false

      - name: Build TotemCore
        shell: bash
        run: |
          set -euo pipefail
          chmod +x gradlew .lockstep/TotemCore/gradlew
          ./.lockstep/TotemCore/gradlew -p .lockstep/TotemCore jar --no-daemon --stacktrace
          core_version="$(sed -n 's/^mod_version=//p' .lockstep/TotemCore/gradle.properties)"
          test "$core_version" = '0.7.0'
          core_jar="${GITHUB_WORKSPACE}/.lockstep/TotemCore/build/libs/totem-core-${core_version}.jar"
          test -f "$core_jar"
          echo "CORE_JAR=$core_jar" >> "$GITHUB_ENV"

      - name: Compile and test Remnant
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew -PtotemCoreJar="$CORE_JAR" \
            test assemble compileGametestJava --no-daemon --stacktrace

      - name: Run server GameTests
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew -PtotemCoreJar="$CORE_JAR" runGameTest --no-daemon --stacktrace

      - name: Verify restart persistence
        shell: bash
        run: |
          set -euo pipefail
          marker_dir="${RUNNER_TEMP}/remnant-restart"
          rm -rf "$marker_dir"
          ./gradlew -PtotemCoreJar="$CORE_JAR" \
            -PtotemRemnantRestartProbePhase=seed \
            -PtotemRemnantRestartProbeMarkerDir="$marker_dir" \
            runRemnantRestartProbe --no-daemon --stacktrace
          test -f "$marker_dir/seed.ok"
          test ! -f "$marker_dir/seed.failure"
          ./gradlew -PtotemCoreJar="$CORE_JAR" \
            -PtotemRemnantRestartProbePhase=verify \
            -PtotemRemnantRestartProbeMarkerDir="$marker_dir" \
            runRemnantRestartProbe --no-daemon --stacktrace
          test -f "$marker_dir/verify.ok"
          test ! -f "$marker_dir/verify.failure"

      - name: Run client GameTests
        shell: bash
        run: |
          set -euo pipefail
          xvfb-run -a ./gradlew -PtotemCoreJar="$CORE_JAR" \
            runClientGameTest --no-daemon --stacktrace
