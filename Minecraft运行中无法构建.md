# 🚨 构建失败：Minecraft 正在运行

## ❌ 错误信息

```
The process cannot access the file because it is being used by another process
- pid: 17928
- command: C:\Users\yunth\.jdks\temurin-21.0.9\bin\java.exe
```

## 🔍 问题原因

**Minecraft 客户端正在运行**，锁定了 Gradle 需要访问的文件：
- `minecraft-common-xxx.jar`
- `mappings.jar`

这通常发生在：
1. 🎮 你在开发环境中运行了 Minecraft 客户端
2. 🎮 通过 IntelliJ IDEA 的 runClient 启动了游戏
3. 🎮 游戏崩溃后进程没有正常退出

## ✅ 解决方案（3 种方法）

### 🚀 方法 1：使用自动脚本（推荐）

我创建了一个脚本来自动终止 Minecraft 并重新构建：

```powershell
.\kill-minecraft-and-rebuild.ps1
```

这个脚本会：
1. ✅ 自动检测运行中的 Java/Minecraft 进程
2. ✅ 显示进程信息（PID、内存使用等）
3. ✅ 询问是否终止进程
4. ✅ 终止后自动执行构建

**一键解决！**

---

### 🔧 方法 2：手动关闭 Minecraft

1. **找到 Minecraft 窗口**
   - 窗口标题：`Minecraft* 1.21.1 - 單人遊戲`

2. **关闭游戏**
   - 按 `ESC` → 离开游戏
   - 或直接点击窗口的 ❌

3. **等待进程完全退出**
   - 等待 3-5 秒

4. **重新构建**
   ```powershell
   .\quick-rebuild.ps1
   ```

---

### 🔨 方法 3：强制终止进程（如果游戏卡住）

```powershell
# 查看所有 Java 进程
Get-Process -Name "java"

# 终止所有 Java 进程
Stop-Process -Name "java" -Force

# 等待几秒
Start-Sleep -Seconds 3

# 重新构建
.\quick-rebuild.ps1
```

⚠️ **注意**：这会终止所有 Java 进程，包括可能运行的其他 Java 程序。

---

## 🎯 推荐流程

```powershell
# 1. 自动终止 Minecraft 并构建（最简单）
.\kill-minecraft-and-rebuild.ps1

# 脚本会：
# → 检测 Minecraft 进程
# → 询问是否终止
# → 自动构建
# → 验证 Gson 是否打包
```

---

## 📋 如果脚本也失败

### 检查是否还有进程占用

```powershell
# 查看所有 Java 进程
Get-Process -Name "java" -ErrorAction SilentlyContinue

# 如果还有进程，强制终止
Stop-Process -Name "java" -Force

# 清理 Gradle 锁文件
Remove-Item ".gradle\loom-cache" -Recurse -Force -ErrorAction SilentlyContinue
```

### 然后重新构建

```powershell
.\gradlew.bat --stop  # 停止 Gradle Daemon
.\quick-rebuild.ps1
```

---

## 🤔 为什么会出现这个问题？

### Gradle Loom 的工作原理

当你运行 Minecraft 开发客户端时：
1. Gradle Loom 会加载 Minecraft 的 JAR 文件
2. 这些文件会被 Java 进程锁定
3. 当你尝试重新构建时，Gradle 需要访问这些文件
4. 但文件被锁定，导致构建失败

### 解决方案

**构建前必须关闭所有 Minecraft 实例**。

---

## 🎬 现在执行

### 最简单的方法：

```powershell
.\kill-minecraft-and-rebuild.ps1
```

脚本会引导你完成所有步骤！

---

## ✅ 构建成功后

如果构建成功，验证脚本会告诉你：
```
✅ 成功！Gson 已打包！
  ✅ 找到 XX 个 Gson 文件
  ✅ JAR 大小: 300-350 KB
```

然后你就可以：
```powershell
# 部署到 Minecraft
Copy-Item "build\libs\deadrecall-1.1.0.jar" -Destination "$env:APPDATA\.minecraft\mods\" -Force
```

---

## 🚫 防止此问题再次发生

### 开发时的最佳实践

1. **测试功能时**：
   - 使用 IDE 的 runClient 启动游戏
   - 测试完成后**立即退出游戏**

2. **准备构建时**：
   - 确保**所有 Minecraft 实例已关闭**
   - 执行构建脚本

3. **使用专用测试环境**：
   - 开发/测试：在 IDE 中运行
   - 打包/部署：使用命令行构建

---

**现在执行：`.\kill-minecraft-and-rebuild.ps1` 解决问题！** 🚀
