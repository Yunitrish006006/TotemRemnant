# 🔍 快速驗證指南

## 問題：PowerShell 解壓 JAR 失敗

你遇到的錯誤：
```
❌ 解壓失敗: .jar is not a supported archive file format
```

**原因**：PowerShell 的 `Expand-Archive` 只識別 `.zip` 擴展名，雖然 JAR 實際上就是 ZIP 格式。

**解決方案**：我已經修復了 `verify-jar.ps1` 腳本。

---

## ✅ 現在重新執行

在 PowerShell 中執行：
```powershell
.\verify-jar.ps1
```

腳本現在會：
1. 將 JAR 臨時複製為 .zip
2. 解壓 .zip 文件
3. 檢查內容
4. 清理臨時文件

---

## 🔧 手動驗證方法（如果腳本還是有問題）

### 方法 1: 使用 7-Zip 或 WinRAR

1. 右鍵點擊 `build\libs\deadrecall-1.1.0.jar`
2. 選擇「解壓縮到...」或「Extract...」
3. 檢查是否有這些目錄：
   - ✅ `com/adaptor/deadrecall/` - 你的模組代碼
   - ✅ `com/google/gson/` - **Gson 庫（重點！）**
   - ✅ `fabric.mod.json`
   - ✅ `deadrecall.mixins.json`

### 方法 2: 手動重命名並解壓

```powershell
# 1. 複製並重命名為 .zip
Copy-Item "build\libs\deadrecall-1.1.0.jar" -Destination "build\libs\deadrecall-1.1.0.zip"

# 2. 解壓
Expand-Archive -Path "build\libs\deadrecall-1.1.0.zip" -DestinationPath "jar_check" -Force

# 3. 檢查 Gson 是否存在
Test-Path "jar_check\com\google\gson"

# 4. 清理
Remove-Item "jar_check" -Recurse -Force
Remove-Item "build\libs\deadrecall-1.1.0.zip" -Force
```

如果 `Test-Path` 返回 `True`，表示 Gson 已正確打包 ✅

---

## 📊 檔案大小檢查

你的 JAR 大小：**260.79 KB** ✅

這個大小是正常的：
- 修復前：~50KB（缺少 Gson）
- 修復後：~260KB（包含 Gson）

**你的 JAR 大小表示 Gson 很可能已經正確打包了！** 🎉

---

## 🚀 直接測試

既然 JAR 大小正常（260KB），直接測試就好：

### 1. 複製到 Minecraft
```powershell
Copy-Item "build\libs\deadrecall-1.1.0.jar" -Destination "$env:APPDATA\.minecraft\mods\" -Force
```

### 2. 啟動 Minecraft
- 使用 Fabric Loader
- 檢查模組列表是否有 DeadRecall

### 3. 測試功能
- 發送聊天訊息
- 使用 `/back` 指令
- 測試死亡座標記錄

### 4. 檢查日誌
```powershell
Get-Content "$env:APPDATA\.minecraft\logs\latest.log" | Select-String "DeadRecall"
```

應該看到：
```
[FabricLoader] Loading mod deadrecall
[DeadRecall] [DiscordBridge] 已啟用，Worker URL: ...
```

如果看到這些，表示模組正常加載 ✅

---

## ❌ 如果出現錯誤

### 錯誤 1: ClassNotFoundException: com.google.gson.JsonParser

**表示**：Gson 沒有正確打包

**解決**：
```powershell
# 確認 build.gradle 有這行
# include 'com.google.code.gson:gson:2.10.1'

# 重新構建
.\gradlew.bat clean
.\gradlew.bat build
```

### 錯誤 2: 模組無法加載

**檢查**：
1. Minecraft 版本是否為 1.21.1
2. 是否安裝 Fabric API
3. 是否使用 Fabric Loader（不是 Forge）

---

## ✅ 成功的標誌

### 日誌中應該看到：
```
[FabricLoader] Loading mod deadrecall 1.1.0
[DeadRecall] [DiscordBridge] 已啟用
```

### 遊戲中可以：
- ✅ 使用 `/back` 指令
- ✅ 聊天訊息轉發到 Discord
- ✅ 死亡時記錄座標

---

## 🎯 總結

基於你的 JAR 大小（260KB），**Gson 很可能已經正確打包了**！

**建議**：
1. 直接複製 JAR 到 Minecraft 測試
2. 如果一切正常，就完成了！🎉
3. 如果還有問題，再檢查日誌

**執行測試命令**：
```powershell
Copy-Item "build\libs\deadrecall-1.1.0.jar" -Destination "$env:APPDATA\.minecraft\mods\" -Force
```

然後啟動 Minecraft 測試！
